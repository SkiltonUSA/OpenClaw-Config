## 2026-03-18 — PDF pipeline buildout and Discord intake automation

- Built a full PDF ingestion pipeline under `pdf-pipeline/` with:
  - Storage layout (`storage/docs/<doc-id>/...`)
  - Clickable indexed HTML library (`storage/index.html`)
  - Manifest-based metadata per document (`manifest.json`)
- Added index generator:
  - `pdf-pipeline/scripts/rebuild_index.py`
- Added ingestion command:
  - `pdf-pipeline/scripts/ingest_pdf.py`
  - Ingests source PDF, writes placeholders/outputs, updates manifest, rebuilds index.
- Added automation for drop-folder processing:
  - `pdf-pipeline/scripts/process_incoming.py`
  - Watches/consumes `incoming/`, writes Discord-ready outbox messages.
- Added Discord intake automation:
  - `pdf-pipeline/scripts/discord_intake_poller.py`
  - Polls intake channel for PDF attachments, downloads to `incoming/`.
  - Configurable deletion of source Discord message after successful download.
- Added operational docs:
  - `docs/README.md`, `docs/DISCORD_FLOW.md`, `docs/HOSTING.md`, `docs/SERVICE.md`, `docs/DISCORD_INTAKE.md`
- Installed and enabled user services:
  - `pdf-pipeline-watcher.service` (continuous ingest)
  - `pdf-pipeline-static.service` (serves clickable index)
  - `pdf-intake-poller.service` (Discord attachment intake)
- Installed parser dependencies:
  - Java runtime (`openjdk-17-jre-headless`)
  - Python venv parser package (`opendataloader-pdf` in `pdf-pipeline/.venv`)
- Wired parser to venv CLI and normalized outputs:
  - Copies `original.md/json` into `parsed.md/json` when parse succeeds.
- Implemented retention policy requested by Dom:
  - Delete source file from `incoming/` after successful ingest.
  - Delete `original.pdf` in storage after successful parse while retaining parsed artifacts and manifest.
  - Optional delete of source Discord intake message enabled (`DELETE_SOURCE_MESSAGE=true`).
- Intake channel configured and validated end-to-end:
  - Intake channel ID: `1480954442349285546`
  - Verified flow: Discord upload -> auto-download -> ingest/parse -> indexed library update -> outbox notice.
- Lifecycle notices posted to corrected operations channel:
  - `1476745103811936319`.
