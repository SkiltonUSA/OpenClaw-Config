# 2026-03-09

- User provided Anthropic API key and asked for setup checks.
- Verified OpenClaw state with `openclaw status` / model docs; confirmed intended routing policy:
  - default model should stay OpenAI Codex
  - `mathew` should use Anthropic.
- After running onboarding/auth updates, observed gateway auth token mismatch symptoms in CLI probes (unauthorized due to stale token context/service embedding), while gateway process itself remained running.
- Verified config shows:
  - `auth.profiles.anthropic:default` present in API key mode
  - default model remains `openai-codex/gpt-5.3-codex`
  - `mathew` model set to Anthropic (`claude-sonnet-4-5`, later updated to `claude-sonnet-4-6`).
- Executed repeated test prompts against `mathew` (`What is 2+2? Reply with only the number.`):
  - failed first with billing/credit balance error
  - then failed with `401 invalid x-api-key`
  - user corrected key/account issue
  - final retest succeeded with response: `4`.
- Upgrade event completed (npm update + gateway restart), followed by doctor run as requested.
- Shared file location for Anthropic credential storage so user could self-edit:
  - `/home/ubuntu/.openclaw/agents/main/agent/auth-profiles.json`
- Security reminder captured: keys pasted in chat/session logs should be rotated after troubleshooting.
