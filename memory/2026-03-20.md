# 2026-03-20

- User asked to harden resilience for intermittent `openai-codex` OAuth refresh failures.
- Updated OpenClaw config: added default model fallbacks under `agents.defaults.model`:
  - primary: `openai-codex/gpt-5.3-codex`
  - fallback 1: `anthropic/claude-sonnet-4-6`
  - fallback 2: `gemini_api/gemini-2.5-pro`
- Gateway restarted automatically by `config.patch` and confirmed success.
- User requested Discord notifications for OAuth failures; posted a manual alert to Discord channel `1481012976688431224` with re-auth steps.
- Implemented automatic detector policy via `HEARTBEAT.md` update:
  - check logs for `OAuth token refresh failed for openai-codex`
  - de-dup via `memory/oauth-monitor-state.json`
  - send Discord alert to channel `1481012976688431224` with reason + re-auth runbook.

- Implemented a separate non-PDF secondary parser pipeline in OpenClaw (MarkItDown-based), parallel to existing PDF pipeline:
  - New scripts: `ingest_nonpdf.py`, `process_incoming_nonpdf.py`, `discord_intake_poller_nonpdf.py`
  - New service units: `pdf-nonpdf-watcher.service`, `pdf-nonpdf-intake-poller.service`
  - New runtime config: `.env-nonpdf`, `.discord-intake-nonpdf.env`
  - Dedicated non-PDF intake channel wired: `1480954466894352634`
  - Output remains on shared HTML index (`pdf-pipeline/storage/index.html`) for unified retrieval.
- Confirmed end-to-end non-PDF intake automation is running and processing files from Discord to indexed storage.
- Calendar operations preference reinforced and applied:
  - Skylight is source-of-truth calendar for schedule/date requests.
  - Must validate required `SKYLIGHT_*` env vars before executing Skylight API commands.
  - Default timezone rule for calendar events: `America/Detroit` (Detroit, MI).
